name: Run reproduction
on: [push, pull_request]

jobs:
  run:
    name: Run
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.0.0

      # run2.sh
      - name: Run
        run: |
          if [ -f run.sh ] ; then
            echo $'------------\n------------\n\n\n'
            bash ./run.sh
          fi
