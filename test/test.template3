import {expect} from 'chai';
import fizzbuzz from '../src/fizzbuzz';

console.log('Required test.ts version 3 (proving that after fixing the TSError, mocha reloads this file)');
describe('fizzbuzz', () => {
  it('input 1 return 1', () => {
    expect(fizzbuzz(1)).to.equal('1');
  });
});