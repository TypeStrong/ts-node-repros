# REPL example with pre-configured imports

This example shows using our REPL API to spawn a REPL with some pre-configured imports.
This means certain types are already available in-scope within the REPL.

