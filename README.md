# jest and native ESM TypeScript

Combine jest and ts-node for native ESM TypeScript support.

<b>NOTE: node had bugs; if this fails, upgrade your node version!</b>

```shell
npm install
npm test
```
