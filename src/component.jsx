import React from 'react';

// ---------------------------------------------------------------------

const TestComponent = ({ message }) => {
  return (
    <div>{message}</div>
  );
};

export default TestComponent;
